spring:
  application:
    name: kotlin-test

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test
    username: sa
    password:

  jpa:
    properties:
      hibernate:
        hbm2ddl.auto: create-drop
        detector:
          enabled: true

logging:
  level:
    org:
      hibernate:
        SQL: debug
